<h2><%= t('.header') %></h2>

<table>
  <tr>
    <th><%= t('.title') %></th>
    <th><%= t('.correct') %></th>
    <th colspan="3"><%= t('.actions') %></th>
  </tr>

  <%= render @test.questions %>

</table>

<p><%= link_to t('.new_question'), new_admin_test_question_path(@test), method: :get %></p>

<%= link_to t('.back'), admin_tests_path %>
<%= link_to t('.edit'), edit_admin_test_path(@test) %>
